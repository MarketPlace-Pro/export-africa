import React, { useState, useEffect } from 'react';
import ProductShowcase from './components/ProductShowcase';

const Home = () => {
  const [searchQuery, setSearchQuery] = useState('');

  const handleSearch = (e) => {
    e.preventDefault();
    console.log('Searching for:', searchQuery);
  };

  return (
    <div style={styles.container}>
      
      {/* HERO - Clean & Bold */}
      <section style={styles.hero}>
        <div style={styles.heroOverlay}>
          <div style={styles.heroContent}>
            
            {/* Main Heading */}
            <div style={styles.heroText}>
              <h1 style={styles.heroTitle}>
                African Trade<br />
                <span style={styles.heroHighlight}>Made Simple</span>
              </h1>
              <p style={styles.heroSubtitle}>
                Connect directly with verified exporters. Source fresh produce, 
                authentic goods, and premium products from Africa's finest suppliers.
              </p>
            </div>

            {/* Search Bar - Modern */}
            <div style={styles.searchSection}>
              <form onSubmit={handleSearch} style={styles.searchForm}>
                <div style={styles.searchWrapper}>
                  <input
                    type="text"
                    placeholder="What are you looking for?"
                    value={searchQuery}
                    onChange={(e) => setSearchQuery(e.target.value)}
                    style={styles.searchInput}
                  />
                  <button type="submit" style={styles.searchBtn}>
                    <span style={styles.searchText}>Search</span>
                    <span style={styles.searchIcon}>🔍</span>
                  </button>
                </div>
              </form>
              
              <div style={styles.searchTags}>
                <span style={styles.tag}>Fresh Fruits</span>
                <span style={styles.tag}>Vegetables</span>
                <span style={styles.tag}>Coffee & Tea</span>
                <span style={styles.tag}>Spices</span>
              </div>
            </div>

            {/* Quick Stats */}
            <div style={styles.quickStats}>
              <div style={styles.stat}>
                <div style={styles.statNumber}>500+</div>
                <div style={styles.statLabel}>Verified Suppliers</div>
              </div>
              <div style={styles.stat}>
                <div style={styles.statNumber}>50+</div>
                <div style={styles.statLabel}>Countries</div>
              </div>
              <div style={styles.stat}>
                <div style={styles.statNumber}>10K+</div>
                <div style={styles.statLabel}>Products</div>
              </div>
            </div>

          </div>
        </div>
      </section>

      {/* FEATURED PRODUCTS - Clean Section */}
      <section style={styles.productsSection}>
        <div style={styles.sectionHeader}>
          <h2 style={styles.sectionTitle}>Featured Products</h2>
          <p style={styles.sectionSubtitle}>Direct from African farms and producers</p>
        </div>
        
        {/* Product Showcase */}
        <div style={styles.showcaseContainer}>
          <ProductShowcase />
        </div>
      </section>

      {/* HOW IT WORKS - Simple Steps */}
      <section style={styles.howItWorks}>
        <div style={styles.sectionHeader}>
          <h2 style={styles.sectionTitle}>How It Works</h2>
          <p style={styles.sectionSubtitle}>Simple, transparent, direct</p>
        </div>
        
        <div style={styles.steps}>
          <div style={styles.step}>
            <div style={styles.stepNumber}>1</div>
            <h3 style={styles.stepTitle}>Browse Products</h3>
            <p style={styles.stepDesc}>Explore verified African products with transparent pricing</p>
          </div>
          
          <div style={styles.step}>
            <div style={styles.stepNumber}>2</div>
            <h3 style={styles.stepTitle}>Connect Directly</h3>
            <p style={styles.stepDesc}>Message suppliers and negotiate terms directly</p>
          </div>
          
          <div style={styles.step}>
            <div style={styles.stepNumber}>3</div>
            <h3 style={styles.stepTitle}>Secure Export</h3>
            <p style={styles.stepDesc}>Manage logistics and track your shipment</p>
          </div>
        </div>
      </section>

      {/* CATEGORIES - Visual Grid */}
      <section style={styles.categories}>
        <div style={styles.sectionHeader}>
          <h2 style={styles.sectionTitle}>Popular Categories</h2>
        </div>
        
        <div style={styles.categoryGrid}>
          <div style={styles.categoryCard}>
            <div style={styles.categoryIcon}>🍊</div>
            <h3 style={styles.categoryName}>Fresh Fruits</h3>
            <p style={styles.categoryCount}>2,400+ products</p>
          </div>
          
          <div style={styles.categoryCard}>
            <div style={styles.categoryIcon}>🥬</div>
            <h3 style={styles.categoryName}>Vegetables</h3>
            <p style={styles.categoryCount}>1,800+ products</p>
          </div>
          
          <div style={styles.categoryCard}>
            <div style={styles.categoryIcon}>☕</div>
            <h3 style={styles.categoryName}>Coffee & Tea</h3>
            <p style={styles.categoryCount}>900+ products</p>
          </div>
          
          <div style={styles.categoryCard}>
            <div style={styles.categoryIcon}>🌰</div>
            <h3 style={styles.categoryName}>Nuts & Spices</h3>
            <p style={styles.categoryCount}>750+ products</p>
          </div>
        </div>
      </section>

    </div>
  );
};

// ========================
// MODERN MOBILE-FIRST STYLES
// ========================

const styles = {
  container: {
    width: '100%',
    minHeight: '100vh',
    margin: 0,
    padding: 0,
    fontFamily: "'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif",
    overflowX: 'hidden',
    backgroundColor: '#ffffff'
  },

  // HERO SECTION
  hero: {
    width: '100%',
    minHeight: '90vh',
    background: 'linear-gradient(135deg, #1e5f38 0%, #2d8b57 100%)',
    position: 'relative',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center'
  },

  heroOverlay: {
    width: '100%',
    height: '100%',
    background: 'rgba(0, 0, 0, 0.1)',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center'
  },

  heroContent: {
    width: '100%',
    maxWidth: '1200px',
    padding: '40px 20px',
    textAlign: 'center',
    color: 'white'
  },

  heroText: {
    marginBottom: '50px'
  },

  heroTitle: {
    fontSize: '2.5rem',
    fontWeight: '800',
    lineHeight: '1.2',
    margin: '0 0 20px 0',
    letterSpacing: '-0.02em'
  },

  heroHighlight: {
    background: 'linear-gradient(45deg, #ffd700, #ff6b35)',
    backgroundClip: 'text',
    WebkitBackgroundClip: 'text',
    color: 'transparent'
  },

  heroSubtitle: {
    fontSize: '1.1rem',
    lineHeight: '1.6',
    opacity: '0.9',
    maxWidth: '600px',
    margin: '0 auto',
    fontWeight: '400'
  },

  // SEARCH SECTION
  searchSection: {
    width: '100%',
    maxWidth: '600px',
    margin: '0 auto 50px auto'
  },

  searchForm: {
    width: '100%',
    marginBottom: '20px'
  },

  searchWrapper: {
    display: 'flex',
    flexDirection: 'column',
    gap: '10px',
    width: '100%'
  },

  searchInput: {
    padding: '18px 20px',
    border: 'none',
    borderRadius: '12px',
    fontSize: '16px',
    width: '100%',
    boxSizing: 'border-box',
    backgroundColor: 'white',
    boxShadow: '0 4px 20px rgba(0, 0, 0, 0.1)',
    outline: 'none'
  },

  searchBtn: {
    padding: '18px 30px',
    background: 'linear-gradient(45deg, #ff6b35, #ff8c42)',
    color: 'white',
    border: 'none',
    borderRadius: '12px',
    fontSize: '1rem',
    fontWeight: '600',
    cursor: 'pointer',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    gap: '8px',
    boxShadow: '0 4px 20px rgba(255, 107, 53, 0.3)'
  },

  searchText: {
    fontSize: '1rem'
  },

  searchIcon: {
    fontSize: '1.1rem'
  },

  searchTags: {
    display: 'flex',
    flexWrap: 'wrap',
    gap: '10px',
    justifyContent: 'center'
  },

  tag: {
    padding: '8px 16px',
    backgroundColor: 'rgba(255, 255, 255, 0.1)',
    borderRadius: '20px',
    fontSize: '0.9rem',
    backdropFilter: 'blur(10px)'
  },

  // QUICK STATS
  quickStats: {
    display: 'flex',
    justifyContent: 'center',
    gap: '40px',
    flexWrap: 'wrap'
  },

  stat: {
    textAlign: 'center'
  },

  statNumber: {
    fontSize: '2rem',
    fontWeight: '800',
    marginBottom: '5px'
  },

  statLabel: {
    fontSize: '0.9rem',
    opacity: '0.8',
    fontWeight: '500'
  },

  // SECTION STYLES
  productsSection: {
    padding: '80px 20px',
    backgroundColor: '#f8fafc'
  },

  howItWorks: {
    padding: '80px 20px',
    backgroundColor: '#ffffff'
  },

  categories: {
    padding: '80px 20px',
    backgroundColor: '#f8fafc'
  },

  sectionHeader: {
    textAlign: 'center',
    marginBottom: '60px',
    maxWidth: '600px',
    margin: '0 auto 60px auto'
  },

  sectionTitle: {
    fontSize: '2.2rem',
    fontWeight: '700',
    color: '#1a202c',
    margin: '0 0 15px 0',
    lineHeight: '1.2'
  },

  sectionSubtitle: {
    fontSize: '1.1rem',
    color: '#718096',
    lineHeight: '1.6',
    margin: 0
  },

  // PRODUCT SHOWCASE CONTAINER
  showcaseContainer: {
    width: '100%',
    maxWidth: '100vw',
    overflow: 'hidden'
  },

  // HOW IT WORKS STEPS
  steps: {
    display: 'flex',
    flexDirection: 'column',
    gap: '40px',
    maxWidth: '1000px',
    margin: '0 auto'
  },

  step: {
    textAlign: 'center',
    padding: '0 20px'
  },

  stepNumber: {
    width: '60px',
    height: '60px',
    borderRadius: '50%',
    background: 'linear-gradient(45deg, #2d8b57, #3cb371)',
    color: 'white',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    fontSize: '1.5rem',
    fontWeight: '700',
    margin: '0 auto 20px auto'
  },

  stepTitle: {
    fontSize: '1.4rem',
    fontWeight: '600',
    color: '#1a202c',
    margin: '0 0 12px 0'
  },

  stepDesc: {
    fontSize: '1rem',
    color: '#718096',
    lineHeight: '1.6',
    margin: 0
  },

  // CATEGORIES GRID
  categoryGrid: {
    display: 'grid',
    gridTemplateColumns: 'repeat(auto-fit, minmax(250px, 1fr))',
    gap: '25px',
    maxWidth: '1000px',
    margin: '0 auto'
  },

  categoryCard: {
    backgroundColor: 'white',
    padding: '30px 20px',
    borderRadius: '16px',
    textAlign: 'center',
    boxShadow: '0 4px 20px rgba(0, 0, 0, 0.08)',
    transition: 'transform 0.2s ease, box-shadow 0.2s ease'
  },

  categoryIcon: {
    fontSize: '3rem',
    marginBottom: '15px'
  },

  categoryName: {
    fontSize: '1.3rem',
    fontWeight: '600',
    color: '#1a202c',
    margin: '0 0 8px 0'
  },

  categoryCount: {
    fontSize: '0.9rem',
    color: '#718096',
    margin: 0
  }
};

// Add desktop media queries
if (typeof document !== 'undefined') {
  const style = document.createElement('style');
  style.textContent = `
    @media (min-width: 768px) {
      /* Hero improvements */
      .hero-title { 
        font-size: 3.5rem !important; 
      }
      
      /* Search row layout */
      .search-wrapper { 
        flex-direction: row !important; 
      }
      .search-input { 
        border-radius: 12px 0 0 12px !important; 
        flex: 1 !important;
      }
      .search-btn { 
        border-radius: 0 12px 12px 0 !important; 
        width: auto !important;
        min-width: 140px !important;
      }
      
      /* Steps row layout */
      .steps { 
        flex-direction: row !important; 
        gap: 60px !important;
      }
      
      /* Stats larger */
      .stat-number { 
        font-size: 2.5rem !important; 
      }
    }

    @media (min-width: 1024px) {
      .hero-title { 
        font-size: 4rem !important; 
      }
      
      .category-card:hover {
        transform: translateY(-5px) !important;
        box-shadow: 0 8px 30px rgba(0, 0, 0, 0.12) !important;
      }
    }

    /* Global image protection */
    img {
      max-width: 100% !important;
      height: auto !important;
      display: block !important;
    }
  `;
  document.head.appendChild(style);

  // Add classes for media queries
  setTimeout(() => {
    const classMap = {
      'hero-title': document.querySelector('[style*="fontSize: 2.5rem"]'),
      'search-wrapper': document.querySelector('[style*="flexDirection: column"]'),
      'search-input': document.querySelector('[style*="padding: 18px 20px"]'),
      'search-btn': document.querySelector('[style*="linear-gradient(45deg, #ff6b35, #ff8c42)"]'),
      'steps': document.querySelector('[style*="flexDirection: column"]'),
      'stat-number': document.querySelectorAll('[style*="fontSize: 2rem"]'),
      'category-card': document.querySelectorAll('[style*="categoryCard"]')
    };

    Object.keys(classMap).forEach(className => {
      const element = classMap[className];
      if (element) {
        if (typeof element.forEach === 'function') {
          element.forEach(el => el.classList.add(className));
        } else {
          element.classList.add(className);
        }
      }
    });
  }, 100);
}

export default Home;
